<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Restaurant" parent="Theme.AppCompat.Light.NoActionBar" />

    <style name="DialogStyle" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:fullBright">@android:color/transparent</item>
        <item name="android:fullDark">@android:color/transparent</item>
        <item name="android:topBright">@android:color/transparent</item>
        <item name="android:topDark">@android:color/transparent</item>
        <item name="android:borderlessButtonStyle">@android:color/transparent</item>
        <item name="android:backgroundDimAmount">0.7</item>
    </style>

    <style name="dialog" parent="@android:style/Theme.Dialog">
        <!--边框-->
        <item name="android:windowFrame">@null</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--半透明-->
        <item name="android:windowIsTranslucent">true</item>
        <!--无标题-->
        <item name="android:windowNoTitle">true</item>
        <!--背景透明这种方法不好使-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFullscreen">true</item>
        <!--模糊-->
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <style name="waitDialog" parent="dialog">
        <item name="android:focusableInTouchMode">false</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <!-- res/values/styles.xml -->
    <style name="CustomSwitch" parent="Widget.AppCompat.CompoundButton.Switch">
        <item name="android:thumb">@drawable/thumb_20dp</item>
        <item name="android:track">@drawable/ios_back_drawable</item>
        <item name="android:textColor">@color/white</item>
    </style>
    <!-- res/values/styles.xml -->
    <style name="CustomSpinner" parent="Widget.AppCompat.Spinner">
        <item name="android:textSize">@dimen/sp_10</item>
        <item name="android:padding">@dimen/dp_4</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@drawable/dialog_corner_bg_shape</item>
        <item name="android:dropDownSelector">@color/theme_green</item>
        <item name="android:spinnerStyle">@style/Widget.AppCompat.Spinner.Underlined</item>
    </style>
    <!--名字自定义-->
    <style name="CheckBoxTheme">
        <item name="colorAccent">@color/theme_color</item><!--选中颜色-->
        <item name="colorControlNormal">@color/white</item><!--未选中颜色-->
    </style>
</resources>